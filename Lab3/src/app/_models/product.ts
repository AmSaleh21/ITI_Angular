export class Product {

  constructor(public id: number, public imagePath: string, public name: string, public code: string, public available: Date, public price: number, public rating: number) { }
}
